{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ClubCompetidores list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Afiliadoel</th>
                <th>Rol</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('clubcompetidores_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.afiliadoel }}</td>
                <td>{{ entity.rol }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('clubcompetidores_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('clubcompetidores_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('clubcompetidores_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
