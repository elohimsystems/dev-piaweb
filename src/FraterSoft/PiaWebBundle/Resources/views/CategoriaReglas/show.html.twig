{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CategoriaReglas</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Atributo</th>
                <td>{{ entity.atributo }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ entity.tipo }}</td>
            </tr>
            <tr>
                <th>Valor1</th>
                <td>{{ entity.valor1 }}</td>
            </tr>
            <tr>
                <th>Valor2</th>
                <td>{{ entity.valor2 }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('categoriareglas') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('categoriareglas_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
