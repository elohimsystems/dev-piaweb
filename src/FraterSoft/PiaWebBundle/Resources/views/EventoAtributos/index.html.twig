{% extends '::base.html.twig' %}

{% block body -%}
    <h1>EventoAtributos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Etiqueta</th>
                <th>Criterio</th>
                <th>Orden</th>
                <th>Busqueda</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('eventoatributos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.etiqueta }}</td>
                <td>{{ entity.criterio }}</td>
                <td>{{ entity.orden }}</td>
                <td>{{ entity.busqueda }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('eventoatributos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('eventoatributos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('eventoatributos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
