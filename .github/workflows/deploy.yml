name: Deploy via FTP

on:
  push:
    branches:
      - develop
      - master

jobs:
  deploy-dev:
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    environment: development

    steps:
      - uses: actions/checkout@v4

      - name: Crear vendor.zip
        run: |
          mkdir -p deploy-vendor
          zip -r deploy-vendor/vendor.zip vendor/

      - name: Subir vendor.zip por FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: deploy-vendor/
          server-dir: /

      - name: Subir vendor.zip como artifact
        uses: actions/upload-artifact@v4
        with:
          name: vendor
          path: deploy-vendor/vendor.zip
          retention-days: 7

      - name: Limpiar
        run: rm -rf deploy-vendor

      - name: Deploy to Development (sin vendor)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./
          server-dir: /
          exclude: |
            .git/**
            .gitignore
            .github/**
            .travis.yml
            composer.json
            composer.lock
            README.md
            LICENSE
            UPGRADE*
            error_log
            phpinfo.php
            vendor/**
